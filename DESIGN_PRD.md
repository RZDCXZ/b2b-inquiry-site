# Torquelis 询盘运营系统｜设计稿产品需求文档

> 本文用于生成与评审 UI 设计稿，不替代业务规格或工程实施计划。功能、权限和数据规则以 `.scratch/torquelis-inquiry-site/spec.md`、`PROJECT_PLAN.md`、`CONTEXT.md` 与 `docs/adr/` 为准。

## 0. 文档信息

| 项目 | 内容 |
| --- | --- |
| 产品名称 | Torquelis Filters／拓擎利滤清多语言询盘演示系统 |
| 产品性质 | 本地运行、数据完全虚构的作品集演示系统 |
| 设计对象 | 英文优先的采购前台 + 简体中文运营后台 |
| 主要使用者 | 海外采购者、业务人员、内容编辑、管理员 |
| 设计目标 | 生成可指导高保真设计稿、交互状态稿、响应式稿和后续前端实现的统一设计依据 |
| 设计基线 | 工业编辑风；深海军蓝、石墨灰、暖白，安全橙仅用于关键操作 |
| 文档状态 | Draft for design generation |
| 更新日期 | 2026-08-13 |

### 0.1 约束标记

- **必须**：来自规格、项目计划、术语表或 ADR，设计不得改变。
- **建议**：为形成完整设计语言而补充的设计方向，可在视觉探索后调整。
- **样例**：仅供设计稿排版的虚构内容，实施时应与固定演示数据对齐。

## 1. 设计任务

设计一套不像“模板企业官网”、也不过度伪装成大型 CRM 的制造业询盘运营系统。设计稿必须让观看者在短时间内看懂两件事：

1. 海外采购者能从结构复杂的产品目录中可靠地找到标准替换件，并带着产品上下文提交询盘。
2. 制造企业团队能把询盘分配、跟进和关闭，并能维护双语内容、预览 Excel 错误、撤销导入和发布产品。

设计稿的第一主角是**产品查找**，第二主角是**询盘时间线**，第三主角是**Excel 导入预览**。工厂介绍、文章、汇总图表和装饰动效不得盖过这三组能力。

## 2. 产品定位与设计叙事

### 2.1 一句话定位

Torquelis 是一个面向商用车滤清器制造企业的多语言询盘运营演示系统：连接产品查找、结构化询盘、业务分配跟进和内容运营。

### 2.2 作品集叙事

设计稿应自然形成以下证据链：

`复杂目录可查找 → 产品信息可判断 → 询盘可结构化提交 → 业务可分配跟进 → 内容与数据可持续维护`

设计不应使用真实客户 Logo、客户评价、认证徽章、检测报告、虚构流量、虚构转化率、虚构销售额或无法验证的工厂资历制造“成功案例”氛围。

### 2.3 体验关键词

- 精确、可靠、克制
- 工业、编辑感、结构化
- 专业但不冰冷
- 数据密集但有清楚层级
- 可信边界持续可见，但不以大型警告条干扰主任务

### 2.4 非目标

- 不设计零售购物、购物车、支付、物流或订单履约。
- 不设计多产品询价车、批量 RFQ 或询盘附件。
- 不设计完整 CRM、独立报价单、客户管理或营销自动化。
- 不设计 VIN 查询、模糊零件号纠错、智能推荐或自定义排序。
- 不设计多层分类树、产品变体、通用页面搭建器或第三方 CMS 体验。
- 不用 SaaS 多租户、公开注册、找回密码或账号管理的视觉语言扩张产品边界。

## 3. 用户与核心任务

| 用户 | 核心任务 | 设计需要帮助其快速回答的问题 |
| --- | --- | --- |
| 海外采购者 | 查找、判断、下载资料、提交询盘 | “是不是这个件？”“适配什么车辆？”“怎么把需求发给供应方？” |
| 业务人员 | 处理分配给自己的询盘、追加联系与报价、安排下一步、关闭 | “我现在该跟进谁？”“已经发生过什么？”“下一步是什么？” |
| 内容编辑 | 维护产品和双语内容、导入 Excel、预览与发布 | “哪些内容可发布？”“这批数据错在哪里？”“撤销是否安全？” |
| 管理员 | 查看全局、分配或重新分配、隔离垃圾询盘、审查记录与设置 | “哪些询盘未处理？”“谁负责？”“系统做过什么变更？” |
| 潜在客户／作品审阅者 | 快速评估产品理解与交付能力 | “这是否接近我的制造业业务，而不只是宣传页？” |

### 3.1 权限在界面中的表达

- 业务人员只看到当前分配给自己的询盘完整内容；重新分配后应进入明确的权限拒绝状态。
- 内容编辑只能看到脱敏的询盘数量，不出现可进入详情的视觉暗示。
- 管理员拥有全局操作，但危险或不可逆操作必须使用二次确认和后果说明。
- 隐藏导航不能作为唯一权限表达；无权访问的直达页面需要独立的权限拒绝稿。

## 4. 体验原则

### 4.1 编号优先，语义明确

产品编号是产品身份，必须比营销名称更显眼。参考号必须明确标为 Cross-reference／参考号，不得与产品编号混排成同一种视觉权重。

### 4.2 找件不猜测

精确产品编号可直达详情；参考号即使唯一匹配也进入结果页；歧义参考号展示全部匹配；无结果不提供“你可能想找”式模糊纠错。

### 4.3 保留上下文

筛选条件、单位和页码可分享、刷新后保留。产品询盘始终显示当前关联产品摘要，避免采购者重复输入产品信息。

### 4.4 状态解释优先于状态颜色

所有状态必须包含文字、必要时包含下一步说明；不得只依赖颜色。导入错误、撤销冲突、权限拒绝和并发冲突要说明原因与可执行的下一步。

### 4.5 历史不可被“漂亮地抹掉”

跟进记录、发布版本和审计日志以时间线或不可变记录表达。更正通过新增记录完成，不出现“编辑历史记录”或“删除审计项”的操作。

### 4.6 演示边界不抢主任务

前台页脚持续说明虚构演示性质；性能数据与 PDF 附近显示更直接的“Demo data — not for selection or purchasing”说明。页眉不放大型警告条。

## 5. 信息架构

### 5.1 采购前台

```text
Home
├── Products
│   ├── Search by part / reference number
│   ├── Search by vehicle
│   ├── Browse by category & specifications
│   ├── Results
│   └── Product detail
├── Private Label
├── Manufacturing & Quality
├── Technical Resources
│   ├── Articles
│   └── Article detail
├── About
├── Contact / General Inquiry
├── Inquiry success
├── Privacy & Demo Data
├── 404
└── System error
```

**必须**：英文路由使用 `/en`，简体中文使用 `/zh-cn`，根地址固定进入英文。语言切换优先保持同一实体与当前任务上下文。

### 5.2 中文运营后台

```text
登录
├── 总览
├── 询盘工作台
│   ├── 询盘列表
│   ├── 询盘详情与时间线
│   ├── 垃圾询盘
│   └── 通知发件箱
├── 产品内容
│   ├── 产品
│   ├── 分类与规格属性
│   ├── 产品资料与图片
│   └── 批量导入与批次历史
├── 内容发布
│   ├── 核心页面
│   ├── 文章
│   ├── 预览
│   └── 发布版本
├── 站点配置
├── 账号与权限说明
└── 审计日志
```

后台不提供账号新增、角色编辑、找回密码、环境安全配置、索引开关或演示数据重置按钮。

## 6. 核心流程

### 6.1 固定采购演示路径

1. 从英文首页进入产品查找。
2. 选择商用车品牌、车型、年份、发动机和分类。
3. 使用当前分类的规格进一步筛选，得到唯一结果。
4. 进入产品详情，切换公制／英制并查看参考号与适配摘要。
5. 下载带演示水印的英文规格 PDF。
6. 打开带产品上下文的询盘表单并提交。
7. 成功页只显示询盘参考号、关联产品编号和通用下一步。

**目标**：从进入产品查找到唯一结果不超过三次关键推进操作；移动端也能完成全部步骤。

### 6.2 编号与参考号分支

| 输入结果 | 页面行为 | 必须表达的信息 |
| --- | --- | --- |
| 精确产品编号 | 直接进入详情 | 产品编号、产品名称与状态 |
| 唯一参考号 | 进入结果页 | “Cross-reference match”及对应虚构品牌 |
| 歧义参考号 | 进入结果页展示全部 | “2 matches found”，不替用户选择 |
| 无匹配 | 保留输入并显示无结果 | 清除输入、改用车型查找、提交通用询盘 |

### 6.3 询盘运营路径

1. 正常询盘进入“待分配”，通知发件箱捕获一条管理员通知。
2. 管理员查看来源与摘要，分配给业务人员。
3. 业务人员追加首次联系记录，状态自动进入“跟进中”。
4. 业务人员追加报价记录，状态自动进入“已报价”。
5. 业务人员设置下一步日期，或关闭并选择成交／未成交／无效。
6. 管理员可重新分配、隔离垃圾询盘或重新打开已关闭询盘。

### 6.4 Excel 内容运营路径

1. 内容编辑下载模板或上传含五个工作表的工作簿。
2. 系统预览新增、更新和全部错误；任一错误阻止确认导入。
3. 编辑下载错误报告并修正工作簿。
4. 再次上传，确认后原子更新草稿，不自动公开。
5. 在未发布且未继续修改时，演示整批撤销。
6. 再次导入，批量预览并发布。
7. 若导入后草稿已发布或再次修改，撤销界面列出冲突对象并拒绝操作。

## 7. 视觉方向

### 7.1 总体风格

采用“工业目录 × 技术编辑部”的视觉语言。前台像经过编辑整理的专业产品目录，后台像清楚、克制的运营工作台。避免常见汽配电商的高饱和促销感，也避免科技 SaaS 的霓虹渐变、重玻璃和浮夸数据大屏。

### 7.2 色彩建议

以下为设计探索起始值，不是工程锁定值；形成设计稿后需做对比度校验。

| Token | 建议值 | 用途 |
| --- | --- | --- |
| Navy 900 | `#10283D` | 主导航、深色区块、主标题 |
| Navy 700 | `#23445E` | 次级标题、选中态、信息图形 |
| Graphite 900 | `#252B2F` | 正文与后台高密度文字 |
| Graphite 600 | `#5B646A` | 辅助文字 |
| Warm 50 | `#F7F4EC` | 前台主背景 |
| Surface | `#FFFFFF` | 卡片、表单、后台内容面 |
| Line | `#D8D5CD` | 分隔线、表格与输入边界 |
| Safety Orange | `#E56A2E` | 主行动、焦点和少量关键提示 |
| Success | `#2E6B4D` | 成功与已发布 |
| Warning | `#9B5C20` | 到期、停产、撤销风险 |
| Danger | `#B4423A` | 错误、垃圾隔离、拒绝状态 |

规则：安全橙不用于大面积背景或装饰；成功、警告、错误均同时使用图标或文字标签；前台深色块不超过页面视觉面积约三分之一。

### 7.3 字体建议

- 英文展示与大编号：`Barlow Condensed` 600，制造工业感但保持可读。
- 英文界面与正文：`Inter` 400/500/600。
- 简体中文界面：`Noto Sans SC` 400/500/600。
- 产品编号、参考号、数值与错误代码：`IBM Plex Mono` 500。
- 若不引入多套字体，优先保证 `Inter / Noto Sans SC` 的统一性，再用字距、大小和色块建立工业感。

### 7.4 版式与密度

- 前台桌面：内容最大宽度建议 1280 px，12 栏，栏距 24 px，页面左右边距 64–80 px。
- 前台移动：以 390 px 设计，4 栏，左右边距 20 px，栏距 16 px。
- 后台桌面：以 1440 px 设计，左侧导航 240 px，顶部工具区 64 px，主内容内边距 32 px。
- 间距序列：4、8、12、16、24、32、48、64、96。
- 圆角克制：输入与按钮 4 px，卡片 6–8 px，状态胶囊可使用全圆角。
- 阴影只用于浮层、抽屉和必要的悬浮层级；卡片主要依靠边框、背景与留白分层。
- 前台留白较大；后台提高表格、筛选和详情信息密度，但保持 44 px 左右的主要交互热区。

### 7.5 图像与图形

- 产品图使用统一角度、暖灰背景、无真实品牌标识的原创或许可明确素材。
- 首页和能力页使用原创滤材剖面、气流路径、尺寸标注等技术线稿；线稿应是真实图片资产，不使用占位框或随意拼接的装饰 SVG。
- 图标使用同一套线性图标库；产品分类图标保持相同视角、线宽和包围盒。
- 不使用真实客户 Logo 墙、五星评价、证书墙、实验室“通过”印章或虚构人物头像。

### 7.6 动效

- 页面进入、导航展开、筛选更新和表单反馈建议 120–220 ms。
- 筛选结果更新可使用轻微淡入和位置过渡，不能让列表顺序产生错觉。
- 尊重 `prefers-reduced-motion`；关闭非必要位移，仅保留即时状态反馈。
- 禁止滚动劫持、背景视频、3D 产品旋转、视差堆叠和持续循环装饰。

## 8. 全局组件规范

### 8.1 前台组件

- 顶部导航：品牌、一级导航、语言切换、询盘行动。
- 产品查找器：三种模式 Tab——Part / Reference、Vehicle、Category & Specs。
- 产品卡片：产品图、产品编号、名称、分类、最多三项关键规格、适配摘要、查看详情。
- 筛选器：桌面侧栏，移动端底部抽屉；已选条件显示为可移除标签。
- 单位切换：Metric / Imperial，明确标识换算值；不可换算字段保持原值。
- 规格表：属性、值、单位；性能数据说明紧邻表格而不是藏在页脚。
- 参考号表：虚构品牌与号码分列，不与产品编号混用。
- 适配表：品牌、车型、年份、发动机分列；移动端转为分组卡片。
- 产品上下文卡：询盘表单和成功页复用，显示产品图、产品编号与名称。
- Demo notice：页脚持续说明；性能数据和 PDF 使用局部强提示。

### 8.2 后台组件

- 左侧导航与角色区：显示当前角色，导航随权限变化。
- 页头：面包屑、页面标题、状态摘要、主要操作。
- 数据表格：固定表头、列显隐克制、稳定空状态、行级操作集中于末列。
- 状态标签：询盘、产品、发布、导入批次分别拥有独立且一致的状态映射。
- 筛选工具栏：搜索、状态、负责人、来源、日期；活动筛选可一键清除。
- 详情摘要栏：关键身份、当前状态、负责人、下一步日期与来源置顶。
- 不可变时间线：联系、报价、内部备注、更正、分配与状态事件按时间展示。
- 表单抽屉／对话框：用于分配、追加记录、关闭、重新打开与垃圾隔离。
- 导入步骤条：上传 → 校验预览 → 确认导入 → 草稿结果。
- 错误表：工作表、行号、字段、错误代码、问题、修正建议。
- Diff 预览：新增、更新、未变化分组；旧值与新值不只靠颜色区分。
- 冲突提示：显示最新修改人、时间、受影响对象和建议下一步。
- 只读标识：通知发件箱、审计日志和历史版本必须清楚表达不可编辑。

### 8.3 通用反馈

| 反馈 | 使用方式 |
| --- | --- |
| Inline validation | 字段下方说明具体问题，提交后焦点移到错误摘要 |
| Error summary | 表单顶部列出错误数量，并提供到字段的锚点 |
| Toast | 只用于已完成且不需用户立即处理的轻量结果 |
| Banner | 用于权限、并发、撤销冲突、停产和演示数据等持续状态 |
| Confirm dialog | 关闭询盘、垃圾隔离、撤销导入、归档、恢复版本等高影响操作 |
| Empty state | 解释为何为空，并只提供与当前角色相关的下一步 |

## 9. 采购前台页面需求

### F-01 全局页头与页脚

**目标**：让采购者随时进入产品查找、切换语言或提交询盘，同时持续看到演示边界。

**页头必须包含**：Torquelis 品牌、Products、Private Label、Manufacturing & Quality、Technical Resources、About、Contact、EN／简中切换。

**页脚必须包含**：中英文企业名称、站点导航、联系信息样例、隐私与演示数据说明入口，以及“Fictional demo manufacturer. All product and performance data are for demonstration only.”的持续声明。

移动端导航使用可键盘关闭的菜单；打开后保留明显的 Products 和 Contact 行动。

前台不设计 Cookie 同意横幅：系统不接入第三方分析或营销脚本，只有后台登录所需的必要会话 Cookie。

### F-02 首页

**目标**：十秒内说明这是“可查找、可询盘”的制造业产品系统，而不是普通形象站。

内容顺序：

1. Hero：行业定位、查找入口、次要询盘行动、原创产品或技术图。
2. 三路径查找器：编号／参考号、车辆、分类规格；默认突出编号输入，同时让三种方式一眼可见。
3. 四个产品分类：Air、Oil、Fuel、Cabin。
4. “From fitment to follow-up”三步说明：Find → Verify → Inquire。
5. 贴牌与包装能力摘要，不使用无法验证的认证表达。
6. 技术资源精选。
7. 克制的演示边界说明与页脚。

**样例英文主文案**：

- Eyebrow: `COMMERCIAL VEHICLE FILTRATION`
- H1: `Find the right filter, without the guesswork.`
- Body: `Search Torquelis part numbers, cross-references or commercial vehicle applications, then send a structured inquiry with the product context attached.`
- Primary CTA: `Find a filter`
- Secondary CTA: `Send a general inquiry`

首页不要出现访问量、客户数量、出口国家数、工厂面积或认证数量等未经验证的数字。

### F-03 产品中心／三路径查找

**目标**：让采购者在一个页面理解三种查找逻辑，并始终知道当前使用的是哪一种。

- Tab A：Part / Reference Number；单输入框，说明忽略大小写、空格和连字符，不做模糊纠错。
- Tab B：Vehicle；按品牌、车型、年份、发动机、分类逐级启用。
- Tab C：Category & Specs；先选四个单层分类，再显示该分类的可筛选规格。
- 桌面端将当前查找条件与结果区域并列；移动端使用分步区块，保持已选摘要固定可见。
- 搜索触发后 URL 中能表达筛选、单位与页码；页面刷新后仍能从界面看见已恢复的条件。

### F-04 搜索结果

**目标**：让用户知道“为什么得到这些结果”，并稳定比较产品。

- 页头显示结果类型、条件摘要、数量和当前单位。
- 每页固定 12 项；只按产品编号稳定排序，不出现“推荐”“最相关”或自定义排序。
- 桌面建议三列或四列卡片，取决于关键规格是否可读；移动端单列。
- 结果卡以产品编号为首要标题，适配与规格作为判断信息，不用营销标签干扰。
- 参考号结果必须显示 `Cross-reference result`；歧义时显示明确说明与全部匹配项。
- 无结果保留全部条件，提供：Clear filters、Search by number、Send a general inquiry。

需出设计状态：默认结果、唯一参考号、歧义参考号、无结果、加载／更新、系统错误。

### F-05 产品详情

**目标**：让采购者判断产品是否适合，并自然进入资料下载或询盘。

首屏信息层级：

1. 面包屑与分类。
2. 产品编号（最高权重）与本地化名称。
3. 产品状态；若已停产，必须在首屏显示并链接替代产品。
4. 产品图。
5. 关键规格与 Metric / Imperial 切换。
6. `Inquire about this product` 主行动与 `Download specification PDF` 次行动。

后续区块：完整规格表、适配摘要、参考号、产品描述、相关产品、资料下载、演示性能数据说明。

**必须**：英制值标为 converted；螺纹与接口等不可换算字段保持原值；性能参数旁显示 `Demo data — not for selection or purchasing.`。

需出设计状态：已发布、已停产且有替代产品、PDF 可下载、PDF 被后台资料替换后的普通状态。

### F-06 产品询盘／通用询盘

**目标**：以最少歧义收集第一次业务沟通需要的信息，不伪装成下单。

产品询盘顶部显示不可编辑的产品上下文卡；通用询盘不显示产品卡，但说明适用于贴牌或尚未找到产品的需求。

必填：姓名、工作邮箱、公司、国家或地区、预计采购数量、留言、隐私同意。

选填：电话或 WhatsApp、目标销售市场、贴牌需求、定制包装需求。

设计要求：

- 必填与选填直接写在标签中，不只使用星号。
- 公共邮箱可提交，不出现企业域名阻断提示。
- 不设计附件上传。
- 提交按钮说明是 `Send inquiry`，不使用 `Place order` 或 `Request official quote`。
- 服务端错误、速率限制和可疑提交只显示安全、可执行的反馈，不暴露风控细节。
- 移动端先显示产品摘要，再显示表单；字段单列。

### F-07 询盘成功

**目标**：确认已收到提交，同时避免共享设备泄露敏感信息或重复创建询盘。

只显示：成功状态、不可推测的询盘参考号、可选的关联产品编号、通用下一步说明、返回产品或产品中心入口。

不显示：姓名、邮箱、电话、公司、留言、公开查询链接或“已发送邮件”承诺。

**样例**：`Inquiry received`、`Reference: TQI-7K4P-92MX`、`Our demo workflow will route this inquiry to an assigned sales representative.`

### F-08 内容页与文章

- Private Label、Manufacturing & Quality、About 使用预设编辑版块，强调方法、可维护内容和适用边界，不堆叠虚构资历。
- Technical Resources 列表显示主题、语言、日期和摘要，不使用虚构阅读量。
- 文章缺少目标语言时，在当前页面显示清楚的暂无版本说明与返回入口，不进入 404，也不拼接另一语言正文。
- 文章详情保持技术编辑排版：窄正文列、明确目录层级、相关产品或通用询盘行动克制出现。

### F-09 隐私、演示说明与错误页

- 隐私与演示数据说明页解释：全部业务数据虚构、本地模拟通知、必要会话 Cookie、默认禁止索引、无真实商业结果。
- 404 提供 Products、Home、General Inquiry 三个恢复入口。
- 系统错误页说明当前操作未完成，可重试或返回；不显示堆栈、数据库信息或内部错误码。

## 10. 中文运营后台页面需求

### B-01 登录

**目标**：明确这是本地演示后台和预置账号入口，不营造公开 SaaS 登录体验。

- 品牌与“运营后台”标题。
- 邮箱、密码、登录按钮。
- 本地演示提示与凭据获取方式的文档引导。
- 不出现注册、找回密码、社交登录或 SSO。
- 需出：默认、字段错误、凭据错误、会话过期。

### B-02 总览

**目标**：显示系统内可验证的待办，而不是营销式仪表盘。

建议布局：

1. 待分配询盘、今日到期跟进、已报价待处理、最近导入批次四个摘要。
2. “需要处理”列表：优先展示待分配与过期跟进。
3. 询盘状态分布与关闭结果，使用简单条形或数字列表。
4. 最近导入与最近发布。
5. 内容编辑角色只看到脱敏汇总和内容待办；业务人员只看到“我的询盘／我的下一步”。

禁止访问量、转化率、销售额、订单额和夸张增长箭头。

### B-03 询盘工作台

**目标**：按状态、负责人、来源与下一步时间定位需要处理的询盘。

主要列：询盘参考号、公司／国家、关联产品编号、状态、当前负责人、来源、提交时间、下一步日期。

- 管理员默认优先显示待分配；业务人员默认显示分配给自己的未关闭询盘。
- 完整联系方式不直接铺在列表中。
- 垃圾询盘使用独立视图，不混入正常状态筛选。
- 空状态要说明当前筛选无结果还是当前角色没有分配任务。
- 需出：默认列表、活动筛选、空状态、垃圾询盘列表。

### B-04 询盘详情与时间线

**目标**：让处理者同时看清当前责任、采购上下文、历史与下一步。

页面结构：

1. 顶部：询盘参考号、状态、负责人、下一步日期、主要操作。
2. 左主栏：采购需求、关联产品、来源页面、完整联系信息（仅有权限者）。
3. 右侧或首屏下方：追加联系、报价、内部备注、关闭等操作。
4. 下方：不可变时间线，包含提交、分配、联系、报价、更正、状态变化与关闭。

报价记录字段：金额、USD／EUR／CNY、有效期、摘要、下一步日期。金额不用汇率换算。

关闭操作必须选择成交、未成交或无效；重新打开仅管理员可见，恢复为“已分配”。重新分配后，旧负责人访问该详情应看到权限拒绝稿，而不是残留的脱敏详情。

需出：待分配、跟进中、已报价、已关闭、追加联系抽屉、报价抽屉、关闭确认、重新分配、权限拒绝、乐观并发冲突。

### B-05 通知发件箱

**目标**：证明通知集成点被捕获，不声称真实邮件已送达。

列：捕获时间、收件角色、模板、关联询盘、状态 `已捕获（本地模拟）`。详情可预览通知内容，但页面顶部持续显示模拟说明；不使用 `Sent`、`Delivered` 或邮箱图标制造真实投递含义。垃圾询盘无通知是可验证的空缺，不需生成占位记录。

### B-06 产品列表与产品编辑

- 列表以产品编号为第一列，显示分类、中文／英文完整度、草稿／已发布／已停产、最近修改人和时间。
- 列表工具区提供模板下载、批量导入与产品数据导出；导出是次级操作，不与新建或导入争夺主行动层级。
- 编辑页按：基础身份、中文内容、英文内容、分类规格、参考号、适配关系、图片与资料、SEO、发布校验分组。
- 中英文使用明确的并列 Tab 或分屏，不在同一富文本区域混排语言。
- 发布按钮附近显示发布资格；缺失项可定位到具体语言与字段。
- 已停产操作说明保留 URL、规格和历史关联，并可选择一个替代产品。
- 需出：列表、双语编辑、发布校验失败、预览、已停产设置、并发冲突。

### B-07 分类与规格属性

**目标**：表达“分类定义驱动表单、Excel、详情和筛选器”，而不是任意字段编辑器。

- 固定四个单层分类，不显示新建分类树入口。
- 属性表显示名称、数据类型、基准单位、选项、必填、可筛选、排序。
- 可换算数值与文本／枚举使用不同字段配置。
- 修改影响已有产品时显示影响摘要和校验结果。

### B-08 Excel 上传与校验

**目标**：让内容编辑在确认前理解工作簿结构和风险。

- 页面顶部说明五个工作表：产品、翻译、规格值、参考号、适配关系。
- 提供下载模板、选择文件、上传与校验。
- 上传后进入独立预览页面，不在小弹窗中承载复杂错误。
- 步骤条始终显示当前位置。
- 文件错误与业务数据错误分开表达。

### B-09 Excel 预览

**目标**：一次看全错误，并让“任一错误阻止整批导入”清楚可见。

顶部摘要：新增数、更新数、错误数、受影响产品数、是否可确认导入。

错误表字段：工作表、行号、字段、错误代码、问题、修正建议。支持按工作表与错误代码筛选，并下载错误报告。

无错误时显示新增／更新 Diff、未出现产品保持不变、导入只更新草稿且不自动发布。确认导入是页面唯一主行动；存在错误时按钮禁用并解释原因。

需出：有多个错误、全部通过、导入处理中、成功更新草稿、事务失败且无部分成功。

### B-10 导入批次与撤销冲突

- 批次详情显示批次号、操作者、时间、文件摘要、新增／更新数量、当前撤销资格。
- 可撤销时，确认对话框说明将整批恢复草稿，不影响未出现产品。
- 不可撤销时列出每个冲突产品、冲突类型（已发布／继续修改）、修改人和时间，并将主行动改为“查看产品／使用发布版本恢复”。
- 不提供逐字段撤销。

### B-11 内容、文章与发布版本

- 核心页面与产品须中英文同时满足条件后发布；文章可按语言独立发布。
- 列表显示每种语言的草稿／已发布／缺失状态。
- 预览使用接近前台真实宽度的预览画布，并明确当前语言与草稿版本。
- 发布历史按不可变版本排列，显示操作者、时间和摘要。
- “恢复”只创建新草稿；确认说明当前公开版本不会立刻变化。
- 富文本工具只提供标题、段落、列表、链接、图片和强调。

### B-12 素材管理

- 图片只接受 JPEG、PNG、WebP；资料只接受 PDF。
- 上传界面要求图片填写中英文替代文本。
- 素材详情显示类型、体积、引用内容、创建时间与版本关系。
- 被已发布内容引用时删除按钮禁用，并列出引用；替换 PDF 创建新素材记录。
- 需出：类型拒绝、缺少替代文本、引用中不可删除、成功创建新记录。

### B-13 站点配置、权限说明与审计日志

- 站点配置只展示企业中英文名称、联系信息、地址、社交链接、默认 SEO 和模拟通知收件角色。
- 页面明确说明索引、数据库与会话密钥由环境配置控制，后台不可修改，但不展示具体秘密值。
- 权限说明使用角色 × 功能矩阵，只读展示预置角色边界。
- 审计日志显示操作人、时间、动作、对象和变更摘要；不出现密码、会话、完整联系方式或留言；无编辑和删除入口。

## 11. 状态与边界矩阵

### 11.1 询盘状态

| 状态 | 视觉语义 | 可用主要操作 |
| --- | --- | --- |
| 待分配 | 需要管理员处理 | 分配、垃圾隔离 |
| 已分配 | 负责人已确定 | 首次联系、重新分配 |
| 跟进中 | 已发生联系 | 追加联系、报价、内部备注、关闭 |
| 已报价 | 已有报价记录 | 追加记录、关闭 |
| 已关闭 | 流程结束并有关闭结果 | 管理员重新打开 |
| 垃圾询盘 | 独立隔离，不进入正常流转 | 管理员审查 |

### 11.2 产品与内容状态

| 对象 | 状态 | 设计重点 |
| --- | --- | --- |
| 产品 | 草稿／已发布／已停产 | 已停产不是删除；前台保留 URL 与替代产品 |
| 核心内容 | 草稿／已发布／归档 | 中英文同时满足发布条件 |
| 文章语言版本 | 缺失／草稿／已发布／归档 | 各语言独立发布，缺失语言不生成路由 |
| 发布版本 | 当前／历史 | 历史不可变；恢复只创建草稿 |

### 11.3 导入状态

| 状态 | 主反馈 |
| --- | --- |
| 待上传 | 工作簿结构说明与模板下载 |
| 校验中 | 明确进度，不允许重复确认 |
| 有错误 | 汇总全部错误，阻止导入，允许下载报告 |
| 可导入 | 显示新增／更新 Diff 与草稿边界 |
| 已导入且可撤销 | 提供整批撤销及后果说明 |
| 已导入且冲突 | 列出冲突对象，拒绝撤销 |
| 事务失败 | 说明未产生部分成功，可重试 |

### 11.4 通用错误状态

设计稿至少区分：字段校验、保存冲突、权限拒绝、资源不存在、速率限制、系统错误。每种状态都需要明确标题、简短原因和一个可执行下一步，不泄露堆栈或敏感信息。

## 12. 多语言与内容规范

- 前台以英文设计为主，并为简体中文检查长度与换行；后台只做简体中文界面。
- 语言切换显示 `EN` 与 `简中`，切换产品时保持同一产品；目标文章缺失时显示暂无版本说明。
- 产品编号、参考号、单位、币种和询盘参考号不翻译。
- 英文日期按英文区域格式，中文前台按中文格式；中文后台固定表达为 Asia/Shanghai 时间。
- 不把中英文放在同一段落中做临时回退。
- CTA 使用任务动词：Find、View product、Download specification、Send inquiry；避免 Learn more 在关键任务中泛用。
- “询盘”不是订单或正式报价单；中文后台统一使用术语表中的“询盘”“报价记录”“当前负责人”“通知发件箱”。

## 13. 响应式与可访问性

### 13.1 关键画布

- 前台桌面：1440 × 1024。
- 前台移动：390 × 844。
- 后台桌面：1440 × 1024。
- 后台窄桌面／平板横向检查：1024 × 768。
- 后台不要求为手机完成高频运营工作，但登录、导航、只读查看和安全退出不能布局破裂。

### 13.2 响应式规则

- 前台导航在移动端折叠；主要查找和询盘行动仍在首屏附近。
- 筛选侧栏在移动端变为底部抽屉，关闭后在结果页显示活动条件标签与结果数量。
- 规格表在移动端保持属性—值对应，不依赖水平滚动理解核心规格。
- 适配关系与参考号表在移动端转为分组列表。
- 后台表格在 1024 px 可隐藏次要列，但必须保留身份、状态、负责人和行动；详情可采用上下分栏。
- 对话框在移动或窄屏下转为全高抽屉。

### 13.3 基础可访问性

- 所有导航、Tab、筛选、表单、对话框和反馈可用键盘完成。
- 焦点样式清晰，建议以安全橙外环配合深色边界，不只改变背景色。
- 文本与背景满足常规可读对比；大字号不能作为低对比度的理由。
- 表单标签永久可见；Placeholder 不代替标签。
- 错误摘要在提交后获得焦点，并可跳转到字段。
- 动态结果更新使用适当的状态提示，不强行移动键盘焦点。
- 图标按钮有可读名称；产品图片和内容图片有对应语言替代文本。
- 颜色之外再使用文字、图标或形状表达状态。

## 14. 设计稿样例数据

以下内容只用于保持多张设计稿的一致性，全部为虚构样例；最终应与演示 fixture 对齐。

| 类型 | 样例值 |
| --- | --- |
| 产品编号 | `TQ-FL-4827` |
| 英文名称 | `High-Efficiency Fuel Filter` |
| 中文名称 | `高效燃油滤清器` |
| 分类 | Fuel Filter／燃油滤清器 |
| 虚构车辆品牌 | `Northline` |
| 车型 | `HX9` |
| 年份 | `2019–2024` |
| 发动机 | `N13-420` |
| 规格 | Outer diameter 96 mm; Height 178 mm; Filtration 10 μm; Rated flow 5.2 L/min |
| 参考号 | `NFX-9081`, `ARV-7710` |
| 询盘参考号 | `TQI-7K4P-92MX` |
| 公司 | `Harborline Fleet Parts` |
| 国家／地区 | Singapore |
| 预计采购量 | `240 pcs` |
| 当前负责人 | `林婧` |
| 报价记录 | `USD 2,880.00`, valid until `2026-09-15` |
| 导入文件 | `torquelis-products-2026-08.xlsx` |
| 导入错误 | `SPEC_UNIT_MISMATCH` — 规格单位与属性定义不一致 |

所有产品页、规格预览和 PDF 设计稿必须同时出现 `Demo data — not for selection or purchasing.` 或等义中文声明。

## 15. 设计稿交付清单

### 15.1 P0：先形成设计语言与主叙事

| ID | 画面 | 画布 | 关键状态 |
| --- | --- | --- | --- |
| P0-01 | 视觉基准页 | 1440 | 色彩、字体、按钮、输入、状态、表格、产品卡 |
| P0-02 | 英文首页 | 1440 | Hero + 三路径查找器 + 四分类 |
| P0-03 | 车型与规格查找结果 | 1440 | 条件摘要 + 唯一结果 |
| P0-04 | 英文产品详情 | 1440 | 公制、规格、适配、PDF、产品询盘行动 |
| P0-05 | 产品询盘 | 1440 | 产品上下文 + 完整表单 |
| P0-06 | 询盘成功 | 1440 | 参考号 + 无敏感回显 |
| P0-07 | 中文后台总览 | 1440 | 数据库可推导的待办与摘要 |
| P0-08 | 询盘详情 | 1440 | 已报价 + 当前负责人 + 不可变时间线 |
| P0-09 | Excel 错误预览 | 1440 | 多错误 + 禁止确认 + 下载报告 |
| P0-10 | Excel 通过预览 | 1440 | 新增／更新 Diff + 确认导入 |
| P0-11 | 英文首页移动版 | 390 | 导航 + 查找入口 |
| P0-12 | 产品筛选移动版 | 390 | 筛选抽屉 + 活动条件 |
| P0-13 | 产品详情移动版 | 390 | 关键规格 + CTA |
| P0-14 | 询盘移动版 | 390 | 产品摘要 + 单列表单 |

### 15.2 P1：补齐关键异常与运营动作

| ID | 画面 | 关键状态 |
| --- | --- | --- |
| P1-01 | 参考号搜索结果 | 歧义参考号，展示全部匹配 |
| P1-02 | 搜索无结果 | 保留条件 + 三个恢复入口 |
| P1-03 | 已停产产品详情 | 替代产品 |
| P1-04 | 登录 | 本地演示说明 + 凭据错误 |
| P1-05 | 询盘工作台 | 管理员待分配视图 |
| P1-06 | 追加报价抽屉 | 金额、币种、有效期、下一步 |
| P1-07 | 关闭询盘确认 | 必选关闭结果 |
| P1-08 | 重新分配后的权限拒绝 | 原负责人无完整访问权 |
| P1-09 | 导入成功批次 | 可整批撤销 |
| P1-10 | 导入撤销冲突 | 列出已发布／继续修改对象 |
| P1-11 | 产品双语编辑 | 发布资格与缺失字段 |
| P1-12 | 版本历史 | 恢复只创建新草稿 |
| P1-13 | 简体中文产品详情 | 与英文为同一产品，检查本地化长度与语言切换 |

### 15.3 P2：完整系统状态

- 通知发件箱及通知预览。
- 垃圾询盘列表与隔离确认。
- 分类与规格属性配置。
- 中英文规格 PDF 模板与持续演示水印。
- 文章列表、英文文章详情和缺失中文版本提示。
- 素材上传、引用中不可删除和 PDF 替换。
- 站点配置、权限矩阵、只读审计日志。
- 隐私与演示数据说明、404、系统错误。
- 1024 px 后台适配检查。

### 15.4 建议的设计文件结构

```text
00 Cover & Decisions
01 Foundations
02 Components — Public
03 Components — Admin
04 Public Desktop
05 Public Mobile
06 Admin Desktop
07 States & Edge Cases
08 Prototype — Buyer Journey
09 Prototype — Inquiry Operations
10 Prototype — Excel Import
```

## 16. 原型连线要求

至少形成三条可点击原型：

1. **采购路径**：首页 → 车型与规格查找 → 唯一结果 → 产品详情 → 单位切换 → 询盘 → 成功。
2. **询盘运营**：总览 → 待分配询盘 → 分配 → 首次联系 → 报价 → 下一步／关闭。
3. **内容运营**：批量导入 → 错误预览 → 通过预览 → 导入草稿 → 撤销 → 再次导入 → 预览发布。

原型中必须可见三类边界：参考号歧义不自动选择、垃圾询盘不触发通知、导入冲突不可撤销。非核心内容页可以只完成主要导航与静态展示。

## 17. 设计验收标准

### 17.1 业务表达

- 首屏能看出这是制造业产品查找与询盘系统，而非零售商城或宣传模板。
- 三条产品查找路径在首页和产品中心都易于发现，且不会互相混淆。
- 产品编号、参考号、适配关系和规格属性拥有清楚且稳定的视觉语义。
- 从产品详情到询盘成功的上下文连续，不需要重复输入产品信息。
- 询盘负责人、状态、历史和下一步在后台详情首屏可判断。
- Excel 错误预览能一次理解“哪里错、为什么错、怎么改、为何不能导入”。
- 发布版本恢复与导入撤销的不同边界不会被设计混为一种“回滚”。

### 17.2 可信边界

- 没有客户 Logo、评价、认证、检测报告或虚构经营成果。
- 前台页脚、性能规格和 PDF 场景都能看见适度的演示说明。
- 通知发件箱只表达“已捕获的本地模拟通知”，不表达真实送达。
- 询盘成功页没有联系方式、公司或留言回显。
- 管理员总览只使用系统数据库可推导的数据。

### 17.3 交互与状态

- 精确产品编号、唯一参考号、歧义参考号和无结果四种结果被分别设计。
- 询盘所有正常状态、垃圾隔离与关闭结果被分别设计。
- Excel 有错误、可导入、成功、可撤销、冲突拒绝和事务失败被分别设计。
- 字段错误、权限拒绝、并发冲突、404 和系统错误有不同反馈。
- 桌面与移动关键路径完整，键盘焦点、错误摘要和减少动态效果有明确规范。

### 17.4 视觉一致性

- 前后台共享品牌字色和基础组件语言，但信息密度明显区分。
- 安全橙只承担行动、焦点与少量关键提示。
- 产品编号、数值、币种和错误代码的排版在所有页面一致。
- 表格、筛选、状态、抽屉、确认和空状态形成可复用组件，不逐页发明新模式。

## 18. 设计生成时的提示词摘要

可将以下摘要作为后续视觉生成的共同前缀，并为每个画面追加本文件对应页面需求：

> Design a high-fidelity responsive interface for Torquelis Filters, a fictional commercial-vehicle filter manufacturer demo. The visual language is industrial editorial: deep navy, graphite, warm off-white, restrained safety orange, generous whitespace on the public site and higher data density in the Chinese admin. Prioritize precise part-number search, vehicle/spec filtering, structured product data, and operational timelines. Use original or licensed filter imagery and technical line art. Do not show customer logos, testimonials, certifications, factory claims, sales metrics, neon gradients, heavy glassmorphism, e-commerce patterns, or generic SaaS dashboard theatrics. Clearly label fictional performance data as demo data not suitable for selection or purchasing.

每次生成单个页面时还应提供：画布尺寸、页面 ID、当前角色、当前状态、样例数据、必须出现的行动和禁止出现的内容；不要只使用上述总提示词批量生成所有画面。

## 19. 假设与待设计阶段确认项

以下事项不阻塞首轮设计稿，但在视觉基准页评审时需要确认：

- Torquelis 最终 Logo、字标比例和中文品牌字样组合尚未定义。
- 色值、字体和圆角为本 PRD 的建议起始值，不是既有品牌资产。
- 设计稿样例车辆、产品与询盘值需要在固定演示 fixture 确定后统一替换。
- 产品图和技术线稿必须在高保真稿前确定生成方式、版权来源与统一拍摄／生成规范。
- 后台以桌面为主要运营环境；手机端不扩张为完整运营承诺。
- 本文不决定具体组件库或前端实现方式，最终实现必须遵守项目技术栈与 ADR。

## 20. 需求追溯

本文主要依据：

- `.scratch/torquelis-inquiry-site/spec.md`：用户故事、实施决策、测试决策、范围与边界。
- `PROJECT_PLAN.md`：定位、首版页面、核心业务规则、演示路径、验收与公开展示顺序。
- `CONTEXT.md`：询盘运营系统、海外采购者、标准替换件、产品编号、参考号、发布版本、导入批次、当前负责人等统一领域术语。
- `docs/adr/0001-versioned-content-and-reversible-imports.md`：发布版本恢复与导入批次撤销的不同交互边界。
- `docs/adr/0002-configurable-category-attributes.md`：规格定义需统一驱动后台表单、Excel、详情和筛选器。
- `docs/adr/0003-modular-monolith-and-local-postgres.md`：前后台属于同一演示系统，但需要清楚的业务模块边界。
- `docs/adr/0004-data-access-auth-and-test-boundaries.md`：真实权限、数据库结果和固定演示路径是设计状态完整性的依据。
