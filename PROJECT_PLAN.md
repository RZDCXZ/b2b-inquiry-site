# 制造企业 B2B 多语言询盘站｜项目方案

## 1. 项目定位

为有产品目录的中小制造企业制作可持续运营的多语言官网，让海外采购者能够找到合适产品、提交结构化询盘，业务人员能够在后台分配和跟进询盘。

演示企业为虚构品牌 `Torquelis Filters／拓擎利滤清`，是一家商用车滤清器制造商，产品覆盖空气滤清器、机油滤清器、燃油滤清器和空调滤清器，主要适配卡车、客车和轻型商用车。企业以标准替换件目录为基础，同时为海外进口商和经销商提供贴牌及包装定制。产品、品牌、证书、客户、询盘和经营数据均为虚构演示数据。公开时标注为“小宇 Mono 个人作品／演示项目”。该名称只经过公开网络的明显撞名初筛，不代表完成正式商标检索或可直接投入真实商业使用。

本项目首先是一套面向中国中小制造企业老板或外贸负责人的本地演示系统，用于证明小宇能够交付制造企业多语言产品目录与询盘闭环，并促使潜在客户预约需求沟通。它所代表的商业服务是带有固定基础能力、再结合客户业务定制的产品化定制服务，不是客户自助使用的 SaaS，也不是通用源码模板。

项目不以公开部署为交付目标。完成后在本地运行并按固定路径演示，通过截图、录屏和案例说明发布到小宇的个人网站。

完整代码仓库公开但不附开源许可证，保留全部权利。陌生人应能根据仓库文档在本地启动系统、载入演示数据并复现固定演示路径。

演示系统使用真实的本地数据库持久化、服务端权限校验、Excel 解析校验和状态历史。邮件等外部服务由明确标注的本地模拟适配器代替，不建设线上监控、云端灾备或生产环境集成。

### 核心证明

- 不只搭建展示页面，还能完成产品数据、询盘、内容后台、性能和可复现的本地交付。
- 能处理多语言、产品筛选、批量导入和结构化 SEO。
- 能清楚区分“技术交付”与流量、排名、询盘、成交等运营结果。

### 完成门槛

项目以可控的交付结果作为完成标准，不把上线后的咨询量作为完成条件：

- 海外采购者通过产品编号、参考号、商用车型或规格查找目标滤清器，并提交带单个产品上下文的询盘。
- 业务人员接收分配并记录联系、报价和下一步，不建设完整 CRM 或独立报价系统。
- 内容编辑通过 Excel 批量维护产品、修正错误并发布。
- 三条路径均可在重置演示数据后重复运行。
- 形成一个 3–5 分钟主视频、关键截图、架构说明和案例文章。

## 2. 启动前决策

开始写代码前确认并写回本节：

- 商用车滤清器各分类的关键规格字段、参考号匹配与按品牌、车型、年份范围和发动机型号表达的适配关系。
- 前台以英文为默认语言并提供简体中文；后台界面仅使用简体中文，但可维护中英文内容。
- 内容管理方案、技术栈和本地运行命令。
- 本地模拟邮件／通知的展示方式和垃圾询盘防护方案。
- 本地运行、演示数据重置和证据采集方式；不为作品集演示单独部署公开站点。
- 以成功标准而非固定时间盒控制范围；不能增强核心证明或演示叙事的功能应延后。

技术底座采用一个 TypeScript 模块化单体：Next.js App Router、React、Tailwind CSS 与 PostgreSQL。前台、简体中文后台和服务端业务模块位于同一应用，代码按产品目录、内容发布、询盘运营、身份权限和通知划分；数据库由 Docker Compose 在本地启动。

Prisma 负责数据库模型、版本化迁移和常规类型安全访问，复杂筛选可使用封装后的参数化 SQL；浏览器表单、URL 参数、Excel 和服务端操作均使用 Zod 在服务端边界重新校验。Better Auth 只启用预置账号所需的邮箱密码登录和数据库会话，密码以哈希保存；业务角色权限由服务端显式判断。

内容管理由同一应用内的业务专用后台完成。核心页面使用预设版块表单；产品描述和文章使用只允许标题、段落、列表、链接、图片与强调的受限富文本编辑器，并支持预览。不接受脚本、内嵌样式、iframe 或任意 HTML，不建设通用拖拽页面搭建器，也不依赖第三方 CMS。

本地运行要求 Node.js、pnpm 和 Docker。陌生人在干净克隆中运行 `pnpm setup`，由脚本创建未纳入 Git 的本地环境文件、生成随机会话密钥、启动 PostgreSQL、执行迁移、生成演示素材并载入演示数据；随后运行 `pnpm dev`。具体运行时与依赖版本在实现时精确锁定。

原创素材源文件、生成脚本和许可清单纳入仓库，初始化时生成轻量产品图片和演示规格 PDF。后台临时上传保存在被 Git 忽略的本地数据目录。`pnpm setup` 为预置账号生成随机本地密码，写入被 Git 忽略的凭据文件并在终端显示；`pnpm demo:credentials` 只在确认本地演示环境后再次显示。`pnpm demo:reset` 仅在确认目标是本地演示数据库后重建演示数据和生成素材，不在后台提供重置按钮。

自动化测试使用 Vitest 覆盖领域规则、真实 PostgreSQL 覆盖事务与查询集成、Playwright 覆盖固定演示路径。GitHub Actions 执行格式检查、Lint、类型检查、全新数据库迁移、单元和集成测试、生产构建，并在 Chromium、Firefox、WebKit 的桌面与移动关键视口运行演示路径。CI 输出覆盖率报告但不设置全局百分比；产品编号匹配、权限矩阵、询盘状态机、导入校验与撤销、单位换算必须覆盖所有分支。

页面保持响应式、语义化、键盘可操作并具有清楚的表单错误提示，但本地演示不设 WCAG 符合等级、Lighthouse 分数门槛或正式性能与可访问性报告。

视觉采用工业编辑风：深海军蓝、石墨灰和暖白为主，安全橙只用于少量关键操作；使用大留白、清晰规格表和原创技术线稿。后台沿用同一字色系统但提高信息密度，不使用营销式动画。动效只服务于导航、筛选结果、表单反馈和页面进入状态，保持短促并遵守系统的减少动态效果设置；不使用滚动劫持、3D 产品或背景视频。

前台页脚和隐私与演示数据说明页持续标注虚构演示企业，所有生成的规格 PDF 带演示水印；不展示客户 Logo、客户评价、认证徽章或无法验证的工厂资历。页面保留完整企业站体验，不在顶部放置干扰演示的大型警告条。

本地默认输出 `noindex` 并通过 Robots 禁止抓取，示例 Canonical 基址使用保留域名 `https://torquelis.example`。只有显式启用 `PUBLIC_INDEXING` 时才产生可索引页面、Hreflang、站点地图和结构化数据，自动化测试覆盖默认与显式启用两种模式。

完成标准：从一个具体产品需求能够推导到唯一的筛选结果，并能形成一条结构化询盘。

## 3. 用户与权限

| 角色 | 主要任务 | 数据边界 |
|---|---|---|
| 海外采购者 | 浏览、筛选、下载资料、提交询盘 | 仅公开内容和自己的提交结果 |
| 业务人员 | 查看分配给自己的询盘、更新跟进状态 | 不修改站点和其他业务人员的私有备注 |
| 内容编辑 | 维护产品、分类、文章和多语言内容 | 不查看完整询盘隐私字段 |
| 管理员 | 分配询盘、处理异常、维护站点配置、查看全局统计与日志 | 全部后台数据 |

海外采购者匿名访问。管理员、内容编辑和业务人员分别使用预置的本地账号登录，权限由服务端强制执行；演示系统不提供注册、找回密码、第三方登录或账号管理功能。

内容编辑可维护并发布产品、分类、核心页面、文章和素材，执行导入、导出与版本恢复；业务人员不能进入内容、导入、设置或审计页面；管理员拥有全部后台操作权限。内容编辑只能看到脱敏的询盘数量，不能打开询盘详情或通知内容。

## 4. 核心业务闭环

采购者进入行业落地页 → 按产品编号、参考号、车型或规格筛选 → 查看产品详情与资料 → 提交带产品上下文的询盘 → 系统校验并记录模拟通知 → 管理员分配业务人员 → 业务人员记录联系、报价、跟进和关闭结果。

内容闭环为：编辑发布产品／文章 → 页面自动生成多语言路由、元数据和站点地图 → 更新后可预览、回滚并记录操作人。

## 5. 首版范围

### 前台网站

- 英文优先、简体中文次要的首页、企业介绍、能力／品控、产品目录、文章和联系页面。
- 空气、机油、燃油、空调四个单层产品分类，以及产品编号、参考号、车型和规格筛选。
- 产品详情、公英制规格切换、相关产品和每个已发布产品的中英文规格 PDF 下载；资料包含产品编号、尺寸、适配摘要、参考号和演示水印，并可在后台替换。
- 最多携带一个产品信息且不支持附件的询盘表单、无产品的通用询盘、成功回执和模拟通知。
- 响应式布局、语义化结构、结构化数据、Canonical、站点地图和 Robots 配置。
- 隐私与演示数据说明、基础可访问性和错误页面；页脚持续标注虚构演示企业，没有非必要 Cookie 时不展示同意横幅。

### 运营后台

- 简体中文后台中的产品、分类、属性、资料和中英文内容管理；产品、分类与核心页面须中英文齐全后才能发布。
- 多工作表产品 Excel 导入、跨表错误预览、整批撤销和导出。
- 询盘列表、分配、有限状态、跟进记录、标签和关闭结果。
- 本地通知发件箱，展示收件角色、模板、创建时间和内容预览，只表示通知已被模拟器捕获。
- 可按语言独立发布的文章／案例内容、SEO 字段、预览、不可变发布版本和旧版本恢复。
- 预置账号权限说明、只读审计日志和只来自演示数据库的运营总览：各状态询盘数、来源页面、待分配数量、到期跟进、关闭结果和最近导入批次。
- 强类型站点配置：企业中英文名称、联系信息、地址、社交链接、默认 SEO 和模拟通知收件角色。
- 本地图片与资料管理：图片仅接受 JPEG、PNG、WebP，资料仅接受 PDF，并执行类型、文件签名、体积、文件名和引用完整性校验。

### 后续功能

- 更多语言和翻译工作流。
- CRM、邮件营销或企业微信集成。
- 经销商资料区和分级下载权限。
- 产品对比、选型器和 RFQ 批量询价。
- 地区化内容、A/B 测试与高级分析。
- 独立报价、客户管理、营销自动化等完整 CRM 能力。
- 真实邮件、对象存储、线上监控、云端备份和生产部署集成。

### 明确不做

- 首版不做跨境零售支付、税务、物流和退换货。
- 不自动抓取或复制其他企业产品资料。
- 不承诺 Google 排名、访问量、询盘量或成交额。
- 不把机器翻译直接当作已审核的专业翻译。
- 不拆分独立前端、API 或微服务，不接入云数据库、外部素材热链或第三方无头 CMS。
- 不建设通用页面搭建器。

## 6. 关键业务规则

1. 每个产品具有稳定唯一的产品编号和语言无关 ID；不同语言页面共享同一业务实体。
2. 发布前检查必填语言、公开状态、URL、元数据和资料文件；草稿不能进入站点地图。
3. 产品变为已停产后保留后台历史、原地址和询盘关联，前台展示替代产品或已停产状态。
4. 询盘保存来源页面、产品、语言和必要联系字段；敏感字段不进入公开日志和分析事件。
5. 表单执行服务端校验、速率限制、蜜罐或等价防垃圾措施；可疑提交单独标记。
6. Excel 导入先预览新增、更新和错误行，确认后执行；产品编号冲突必须明确提示。
7. 询盘分配、状态变化和内部备注保留操作历史。
8. 多语言缺失内容使用明确回退策略，不能把不同语言拼接在同一页面。
9. 产品、分类和核心页面只有在中英文均满足发布条件时才生成两种语言路由；文章按语言独立发布，缺失语言不生成路由或站点地图记录。
10. 已发布内容保留不可变版本并可恢复旧版本；确认执行的 Excel 导入形成一个只更新草稿的原子批次，仅在受影响草稿尚未发布且未继续修改时可整批撤销，不支持逐字段撤销。
11. 每个可供应零件对应一个稳定产品编号，不建立产品变体；一个产品可关联多个参考号和多条适配关系。
12. 适配关系由商用车品牌、车型、适用年份范围和发动机型号组成，不支持 VIN 查询。
13. 产品状态为草稿、已发布或已停产；已停产产品保留原 URL、历史询盘和规格，并可关联一个替代产品。
14. 询盘依次处于待分配、已分配、跟进中、已报价或已关闭状态；关闭时必须记录成交、未成交或无效之一。垃圾询盘单独隔离，不进入正常状态流转。
15. 前台提供三条产品查找路径：按产品编号或参考号查找、按商用车型查找、按分类与规格筛选。编号查找忽略大小写、空格和连字符，精确结果优先，不进行编辑距离纠错。
16. 管理员可按分类配置规格属性的名称、数据类型、单位、选项、必填性和可筛选性；同一套属性定义驱动后台表单、Excel 校验、前台规格表和筛选器。
17. 询盘必填姓名、工作邮箱、公司、国家或地区、预计采购数量、留言和隐私同意；电话或 WhatsApp、目标销售市场、贴牌或包装需求选填，不强制使用企业域名邮箱。
18. 一张询盘只有当前负责人可查看完整联系信息和内部记录。重新分配后前负责人立即失去访问权，但历史操作继续保留；管理员可查看全部，内容编辑只能查看脱敏汇总。
19. 产品地址使用 `/{语言}/products/{产品编号}-{本地化名称}`；产品编号确定产品身份，本地化名称变化时旧地址永久重定向到当前地址。
20. 可换算的数值规格以公制基准值保存，前台默认显示公制并可切换英制换算值；螺纹型号等不可换算内容使用文本或枚举，换算结果须明确标识。
21. 过滤精度、流量和效率等性能参数只能使用合理范围内的虚构演示值，并在页面、资料和截图中标注“演示数据，不可用于选型或采购”；不虚构认证、检测报告或合规声明。
22. 产品 Excel 工作簿包含产品、翻译、规格值、参考号和适配关系五个工作表，各表通过产品编号关联并作为一个导入批次校验、确认和撤销。
23. 正常询盘提交使用蜜罐、最短填写时间、服务端速率限制、一次性提交令牌和简单风险规则；可疑提交进入垃圾询盘区且不触发通知。
24. 跟进记录分为联系、报价和内部备注，保存发生时间、操作人、摘要及下一步日期；报价记录另含金额、币种和有效期，但不生成报价单。已保存记录不可覆盖，错误通过追加更正记录处理。
25. 初始规格属性如下：空气滤清器使用外径、内径、高度、滤材类型和额定空气流量；机油滤清器使用结构形式、外径、内径、高度、螺纹规格、旁通阀开启压力和止回阀；燃油滤清器使用结构形式、外径、高度、接口规格、过滤精度、额定流量和油水分离；空调滤清器使用长度、宽度、高度、滤材类型和额定空气流量。
26. 产品编号标准化后全站唯一。参考号记录所属虚构品牌，允许同一标准化号码匹配多个产品；查找时产品编号优先，参考号歧义必须展示全部结果，不擅自选取一个。
27. Excel 以产品编号作为跨表身份键：存在即更新、不存在即新增、未出现即保持不变，不隐式删除或停产。任一错误阻止整批执行但预览列出全部错误；若受影响草稿在导入后又被发布或修改，拒绝整批撤销并列出冲突。
28. 正常询盘提交后为待分配；管理员分配后为已分配；首次联系后为跟进中；添加报价记录后为已报价。当前负责人可关闭并选择关闭结果，只有管理员可重新分配、隔离垃圾询盘或重新打开，重新打开后回到已分配。
29. 正常询盘产生一条面向管理员的模拟通知，分配后产生一条面向当前负责人的模拟通知，垃圾询盘不产生通知。通知发件箱只标记已捕获，不声称真实送达。
30. 演示系统不接入第三方分析或营销脚本，只使用后台登录所需的必要会话 Cookie；前台不展示 Cookie 同意横幅，保留隐私与演示数据说明页。
31. 所有公开页面、规格 PDF、截图和录屏均不得把演示企业或演示性能数据表达为真实客户、认证、实验结果或经营成果。
32. 8 个原创文章主题中，6 个同时发布中英文，2 个只发布英文，用于验证缺失语言不生成路由或站点地图记录。
33. 后台总览只展示可从演示数据库推导的询盘、跟进和导入指标，不展示虚构访问量、转化率、销售额或其他外部经营成果。
34. 产品公开表示、分类、核心页面和文章每次发布均形成不可变版本；恢复旧版本只创建新草稿，必须重新预览和发布后才生效。询盘、跟进、账号和审计记录不属于内容版本。
35. 可变草稿、询盘分配和状态操作使用版本号进行乐观并发检查；旧版本操作被拒绝，并提示最新修改人和时间，不使用长期记录锁。
36. 审计日志记录登录结果和业务变更，包括发布与恢复、导入与撤销、产品停产、询盘分配与重新打开或隔离、跟进追加和站点设置修改。日志保存操作人、时间、动作、对象和变更摘要，不复制密码、会话、完整联系方式或留言；后台不提供编辑或删除入口。
37. 后台只允许维护明确列出的强类型站点配置；索引开关、数据库地址、会话密钥和其他安全或环境配置只能通过环境变量控制。
38. 产品分类只使用空气、机油、燃油和空调四个单层分类，不建设分类树。
39. 后台图片仅接受 JPEG、PNG、WebP，资料仅接受 PDF；上传同时检查声明 MIME 与文件签名、限制体积并生成安全文件名。图片必须填写中英文替代文本，被已发布内容引用的素材不能删除；替换资料创建新素材记录，不静默覆盖旧文件。
40. 英文使用 `/en`，简体中文使用 `/zh-cn`，根地址始终重定向到 `/en`，不按浏览器语言自动跳转。语言切换优先进入同一实体的另一语言页面；文章缺失目标语言时明确标注暂无版本，不拼接内容或进入 404。
41. 询盘提交成功后生成不可推测的参考号，成功页只显示参考号、关联产品编号和通用下一步说明，不显示联系方式、留言或公开查询入口。提交使用 POST/Redirect/GET；一次性令牌的重复请求返回原成功结果，不创建第二张询盘。
42. 精确产品编号直接进入产品详情；参考号匹配进入结果页并区分唯一或歧义结果；车型和规格结果按产品编号稳定排序。每页 12 项，筛选、单位和页码写入 URL，不提供自定义排序或智能相关性。
43. 时间在数据库中使用 UTC，英文和中文前台按各自语言格式显示，简体中文后台固定使用 `Asia/Shanghai`。报价金额使用十进制定点数和 ISO 4217 币种代码，演示数据只使用 USD、EUR、CNY，不做汇率换算。
44. 已发布产品、内容、询盘、跟进、导入批次和审计日志不能硬删除；产品使用已停产状态，其他内容使用归档状态。只有从未发布且没有引用或历史的草稿可以永久删除。
45. Excel 导入只更新产品草稿，不自动公开。受影响草稿尚未发布且未继续修改时，导入批次可以整批撤销；任一受影响产品在导入后发布或再次修改时，该批次即不可撤销，公开内容只能通过恢复发布版本处理。
46. 搜索无结果时保留筛选条件，并提供清除筛选、改用编号查找和提交通用询盘三个动作。后台空列表必须解释当前状态并给出适用的下一步。
47. 导入错误包含工作表、行号、字段、错误代码和修正建议，并可下载错误报告。保存冲突、权限拒绝、资源不存在和系统错误使用不同反馈，不向界面泄露堆栈或敏感信息。

## 7. 页面与数据

### 前台页面

- 首页，主要动作是查找滤清器，次要动作是提交询盘。
- 产品中心：编号查找、车型查找、分类与规格筛选、产品详情和资料下载。
- 贴牌服务、制造与品控、技术资源、关于我们。
- 文章列表、文章详情、联系／通用询盘与询盘结果页。
- 隐私与演示数据说明、404 和系统错误页。

### 后台页面

- 内容总览。
- 产品、四个单层分类、属性、资料和批量导入。
- 文章和多语言发布。
- 询盘工作台、详情、分配和跟进。
- 预置账号权限说明、只读审计日志、表单来源和强类型站点配置。

### 核心数据实体

`Product`、`ProductTranslation`、`ProductReference`、`Category`、`AttributeDefinition`、`AttributeOption`、`ProductAttributeValue`、`VehicleMake`、`VehicleModel`、`Engine`、`Fitment`、`DocumentAsset`、`Article`、`ContentRevision`、`ImportBatch`、`Inquiry`、`InquiryAssignment`、`FollowUp`、`NotificationOutbox`、`SiteSettings`、`User`、`Role`、`AuditLog`。一张 `Inquiry` 最多直接关联一个 `Product`，不建立产品变体或多条目询价模型。

## 8. 演示数据与演示路径

准备 4 个产品分类、50 个产品、6 个虚构商用车品牌、12 个车型、12 个发动机、大约 150 条适配关系、每个产品 2–4 个虚构参考号、8 个原创文章主题、每个已发布产品的中英文规格 PDF、两种语言和 20 条不同来源／状态的询盘。6 个文章主题同时发布中英文，2 个只发布英文。至少一个固定场景可由车型、年份、发动机、分类和规格推导到唯一产品。产品图片和资料必须自制、获授权或使用许可明确的素材。

固定演示路径：

1. 海外采购者按商用车型和规格筛选到目标滤清器。
2. 在产品详情下载规格资料并提交询盘。
3. 管理员收到通知，把询盘分配给业务人员。
4. 业务人员记录首次联系、报价和下一步。
5. 内容编辑展示 Excel 错误预览，修正后导入草稿并整批撤销，再次导入后批量预览和发布。
6. 展示移动端、页面元数据、演示数据重置和干净克隆后的本地启动。

完成标准：重置数据后可以重复演示，邮件使用明确标注的本地模拟通道。

## 9. 实施步骤与完成标准

1. **本地底座**：完成应用、PostgreSQL、迁移、预置账号认证、语言路由、演示凭据和安全数据重置；在 macOS 人工验证，在 Linux CI 验证。
2. **产品发现**：完成产品模型、车型与参考号、规格筛选、详情、双语规格 PDF 和确定的筛选 URL；采购者能用不超过三步找到固定目标产品。
3. **询盘闭环**：完成提交、防垃圾、通知发件箱、分配、跟进、报价和关闭；使用真实权限与数据库跑通完整路径。
4. **内容运营**：完成核心页面与文章、发布版本、素材、Excel 错误预览、草稿导入、批次撤销和批量发布。
5. **公开证据**：完成三浏览器 CI、README 与技术文档、干净克隆验证、关键截图、3–5 分钟主视频和案例文章。

每个切片都必须保持应用可运行并通过该阶段的自动化测试，不先制作无法与真实数据和权限串联的静态页面。macOS 是人工演示环境，Linux 是 GitHub Actions 自动化验证环境；Windows 可以尝试运行，但不是项目完成条件。

若投入明显超过作品集证明价值，依次削减装饰动效与非必要视觉细节、后台汇总图表、文章数量和演示数据数量。文章最少保留 4 个主题及缺失语言场景；数据缩减后仍须覆盖所有关系类型、异常样例和固定路径。不得削减产品查找、询盘闭环、服务端权限、Excel 错误预览、内容发布恢复和干净克隆启动。

## 10. 项目验收标准

- 中文和英文页面路由、切换、元数据与站点地图对应正确。
- 产品筛选结果与固定测试数据一致，50 个产品导入可以指出每一条错误。
- 产品编号和参考号的标准化匹配、车型级联选择、分类规格筛选以及旧产品地址重定向均有固定测试数据覆盖。
- 询盘从前台提交到后台跟进全程可追溯，业务人员不能查看未分配询盘的私有信息。
- 表单的正常、重复、超速和可疑提交均有确定结果；垃圾询盘不触发通知，首版不接受附件。
- 导入批次在未发布草稿上可整批撤销；一旦受影响草稿发布或后续修改，撤销被拒绝并列出冲突对象。
- 固定演示路径在 Chromium、Firefox 和 WebKit 的桌面与移动关键视口通过自动化测试；不设 Lighthouse 或 WCAG 评分型验收。
- 陌生人可根据公开文档在干净克隆中安装依赖、初始化数据库、载入演示数据并运行全部固定演示路径。
- `pnpm demo:reset` 对非本地或身份不明的数据库拒绝执行，并能恢复预定的产品、询盘和生成素材状态。
- 作品性质、素材许可、翻译状态和不承诺运营结果的边界清楚可见。
- 默认模式不可索引；显式开启 `PUBLIC_INDEXING` 后，Canonical、Hreflang、结构化数据和站点地图与已发布语言路由一致。
- 公开仓库包含定位与虚构声明、本地启动、演示凭据获取、架构边界、数据模型、术语表、权限矩阵、Excel 模板与字段字典、导入错误代码、固定演示脚本、重置说明、素材许可清单和已知边界。

## 11. 公开展示与服务承接

### 推荐展示材料

- 海外采购者从 50 个产品中快速找到适配型号并提交询盘。
- 普通企业官网与“可筛选、可跟进、可维护”询盘站的区别。
- 批量产品导入的错误预览和修正过程。
- 多语言、数据迁移和可复现交付为什么影响长期维护成本。

主视频控制在 3–5 分钟，使用中文旁白与简短中文字幕：先在英文前台按车型找到唯一产品、切换单位并查看双语页面和规格 PDF，再提交询盘；随后在中文后台展示模拟通知、管理员分配、业务人员联系与报价、内容编辑的 Excel 错误预览／撤销／发布，最后展示自动化检查、架构图和本地重置。案例文章承载更完整的技术细节。

### 可承接服务

- 企业官网、外贸 B2B 询盘站和产品目录后台。
- 多语言、产品迁移、表单、邮件和 CRM 接口。
- 网站改版、性能优化、部署迁移、备份和长期维护。
- SEO 技术基础与内容后台；不承诺排名和询盘结果。

## 12. 风险与边界

- 产品参数、认证、品牌和翻译必须由客户确认；开发方不替客户声称行业资质。
- 海外隐私、Cookie、数据存放、营销邮件和跨境传输要求随目标市场变化，正式项目需单独核对。
- 本项目只证明约定本地环境和自动化浏览器中的正常运行，不把本机结果包装成生产环境性能承诺。
- 演示系统中的模拟外部服务不能被描述为已经完成真实生产集成；案例材料需明确两者边界。
