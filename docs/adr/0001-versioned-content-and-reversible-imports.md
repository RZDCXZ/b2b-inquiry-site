# 发布内容版本化，产品导入按批次撤销

产品公开表示、分类、核心页面和文章保存不可变发布版本；恢复旧版本只创建新草稿，必须再次预览和发布，不直接改变当前公开内容。确认后的 Excel 产品导入作为原子批次记录，只更新草稿且不自动公开；工作簿的产品、翻译、规格值、参考号和适配关系通过产品编号关联，未出现的产品不受影响，任一错误阻止整批执行。只有受影响草稿尚未发布且未继续修改时才允许整批撤销，否则拒绝并报告导入冲突；已发布内容只能通过发布版本恢复。相比仅保留审计日志，这能真实展示内容恢复能力；相比逐字段事件溯源，它显著降低了演示系统的模型和交互复杂度，同时仍为关键运营失误提供清晰的恢复边界。
