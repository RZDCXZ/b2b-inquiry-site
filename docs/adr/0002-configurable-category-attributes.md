# 使用分类可配置的强类型规格属性

产品规格采用由分类配置的属性定义，每项定义包含数据类型、单位、选项、必填性和可筛选性，并统一驱动后台表单、Excel 校验、产品详情和前台筛选。可换算数值以公制基准值保存，英制仅作为明确标识的派生显示值，不可换算的螺纹型号等使用文本或枚举。相比为四类滤清器写死不同字段，这种模型更符合产品化定制服务的复用目标；相比任意 JSON，它保留了导入校验、单位换算和可靠筛选所需的结构，同时接受更复杂的数据约束与查询实现。
