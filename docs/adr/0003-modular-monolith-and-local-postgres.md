# 使用模块化单体、PostgreSQL和业务专用后台

系统使用一个基于 Next.js、React 和 TypeScript 的模块化单体承载前台、简体中文后台与服务端业务模块，并通过 Docker Compose 运行本地 PostgreSQL；内容管理作为同一应用内的业务专用后台实现。相比拆分前端、API、微服务或接入第三方 CMS，这一结构让公开仓库可以一键本地复现，并保留事务导入、复杂目录关系和服务端权限所需的完整后端证据；代价是各业务模块必须在同一进程中保持明确边界，未来真实项目若需独立扩缩容则需要拆分。
